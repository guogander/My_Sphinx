=============
目录
=============

*toctree*

.. code-block:: text

    本指令在当前位置插入一个“目录树”，使用指令正文中给出的文件的单个目录（包括“子目录树”）。

    相对文档名（不是以斜杠开头）相对于指令所在的文档，绝对名称相对于源目录。
    
    可以提供一个数字的“maxdepth”选项来指示树的深度；默认情况下，包括所有级别。


几个常用参数如下：

.. code-block:: text

    :maxdepth:2      # 设置最大深度
    :numbered:       # 自动编号
    :caption:        # 目录标题
    :name: 标签      # 名字，可以通过以下方式引用的隐式目标名称：  :ref:`标签`   例下面引用的指令
    :titlesonly:     # 不显示除了一级标题以外的标题
    :glob:           # 通配符，这样写文件条目简单写
    :reversed:       # 反向编号
    :hidden:         # 隐藏


:ref:`指令`